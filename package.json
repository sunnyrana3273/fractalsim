{
  "name": "hand-tracker-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:backend": "source venv/bin/activate && python app.py || python app.py",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev\"",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "concurrently": "^8.2.2",
    "vite": "^5.0.0",
    "typescript": "^5.2.2"
  }
}

